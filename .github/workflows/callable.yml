name: Callable Workflow
on:
  workflow_call:
    # inputs:
      # boolean:
      #   description: A test boolean
      #   type: boolean
      #   required: true
      #   default: false
      # string:
      #   description: A test string
      #   type: string 
      #   required: true
      #   default: "default"
jobs:
  workspace:
    runs-on: ubuntu-latest
    outputs:
      plan: ${{ steps.terraform.outputs.plan_json }}
    steps:
      - name: Show me the money!
        shell: bash
        run: | 
          echo boolean: ${{ github.event.inputs.boolean == 'true' || github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
          echo string: "${{ github.events.inputs.string || 'default' }}"
